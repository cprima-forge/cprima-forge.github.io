{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://forge.cprima.net/api/v1/schemas/product.schema.json",
  "title": "catalog product",
  "description": "One sellable product, in data/products/<product>.yaml. A product is a thing a buyer recognises; an offer is one way to buy it. The two are separate because the same product is commonly sold as a free edition and a paid one, and because a price change must not require restating what the product is.",
  "type": "object",
  "required": ["product", "name", "summary", "artefacts", "offers"],
  "additionalProperties": false,
  "properties": {
    "product": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Stable identifier, matching the filename. Never reused for something else once an order has referenced it."
    },
    "name": { "type": "string", "description": "How it is written on the storefront." },
    "summary": { "type": "string", "description": "One or two sentences. What it does, not how." },
    "licensed": {
      "type": "boolean",
      "description": "Licences are issued for offers of this product. Nothing more — it does NOT mean 'requires a key at runtime', which is a client-enforcement decision the catalog has no business making: a licenced product may be activated by a pasted key, an account token, a shared licence store, or nothing at all. Absent means false, and an unlicenced product is skipped by the licencing reconciler rather than silently ignored. THE FIELD NAME KEEPS THE AMERICAN SPELLING and must not be 'corrected' to licenced: it is a wire value in the published /api/v1/catalog.json contract, which keygen_sync.py reads to decide what to reconcile. A repository-wide rename did change it once, and every product then read as unlicensed — all ten managed objects reported as drift, from a change that looked like a spelling fix."
    },
    "policies": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9.-]*$" },
      "description": "The licencing tiers ASSIGNED to this product, by template name. This is the complete set, not a subtraction from a default: a licenced product has exactly these policies and no others, and one with an empty or absent list has none. Nothing is granted implicitly.\n\nThese are OUR names, defined in the licencing repository's policy templates — never a provider's UUID and never a provider's attributes, so no vendor identity enters the catalog. A provider swap replaces what the names mean and leaves this list untouched.\n\nAssigning a tier to a product is therefore a data edit, and so is withdrawing one.",
      "examples": [["free", "plus"], ["pre-v0.5"]]
    },
    "artefacts": {
      "type": "array",
      "minItems": 1,
      "description": "What is actually delivered, by the identity the producing repo publishes under. This is the join to manifest.json and to doc.json, and it is why a SKU can be checked against something that really exists.",
      "items": {
        "type": "object",
        "required": ["id", "kind", "format"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "description": "OUR stable identity for this artefact. Externally assigned identities live under `identifiers`, never here."
          },
          "kind": {
            "type": "string",
            "enum": ["software", "publication"],
            "description": "What sort of thing it is. Previously this field held vsix/nupkg/installer, which are formats and a software-only vocabulary — so nothing that was not a software package could be described at all."
          },
          "format": {
            "type": "string",
            "enum": ["vsix", "nupkg", "installer", "epub", "pdf", "paperback", "hardcover"],
            "description": "The concrete form it takes. Formats are added as real artefacts require them, not in anticipation."
          },
          "fulfillment": {
            "type": "string",
            "enum": ["download", "shipment"],
            "description": "How it reaches the holder. Deliberately NOT the top-level taxonomy: a book is one product with both downloadable and shipped artefacts, so digital-versus-physical is a property of the artefact and never of the product."
          },
          "identifiers": {
            "type": "object",
            "additionalProperties": { "type": "string" },
            "description": "Identities assigned by somebody else, namespaced by who assigned them — isbn, gtin, vscode-marketplace. Our own id stays ours; a foreign identity is a join to their system, not a replacement for it.",
            "examples": [{ "isbn": "9781234567890" }, { "vscode-marketplace": "cpmforge.cpmf-workflow-lens" }]
          },
          "edition": { "type": "string", "enum": ["open", "plus"], "description": "Mirrors libraries.yaml. Omit where the product has no edition split." }
        }
      }
    },
    "offers": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/offer" }
    }
  },
  "$defs": {
    "offer": {
      "type": "object",
      "required": ["sku", "name", "price"],
      "additionalProperties": false,
      "description": "One way to obtain one product. The SKU is what an order references and what a downstream capability maps to, so it is permanent once used.\n\nThe required core is deliberately tiny: an identity, a label, a price. `duration`, `renewal`, `seat_model`, `seats`, `eligible_for` and `grants` are LICENSING terms and are optional — requiring them forced every offer to be shaped like a software licence, so a paperback had to invent a seat model and a duration in order to exist at all. An offer is a way to obtain something; it is not necessarily a way to licence something.",
      "properties": {
        "sku": {
          "type": "string",
          "pattern": "^[A-HJKMNP-TV-Z][0-9A-HJKMNP-TV-Z]{7}$",
          "description": "Permanent, opaque, checksum-protected. Eight uppercase characters from the Crockford Base32 alphabet (no I, L, O or U), first a letter, last an ISO 7064 MOD 37-2 check character. See docs/sku-format.md; allocate with tools/sku.py.\n\nThis pattern is LEXICAL only — the checksum is verified by tools/validate.py, because a regex cannot compute one.\n\nOpaque on purpose: a readable id asserts facts it cannot retract. `workflow-lens-free` claimed a product, a tier and a price, all of which are stated separately in fields that may change, and it invited derivation — a function once manufactured `cpmforge-licence-spike-trial` from a display name, producing an identifier that looked plausible and joined to nothing. Changing a SKU orphans every order that referenced it; retire it and add a new one."
        },
        "slug": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Human-readable identity for logs, URLs and operators. Free to change, unlike the SKU — which is the entire point of separating them. Never used as a join."
        },
        "name": { "type": "string" },
        "price": {
          "type": "object",
          "required": ["currency"],
          "additionalProperties": false,
          "description": "Exactly one of `amount` or `status`. A price is either decided — including decided as zero — or explicitly not decided. It is never both and never neither.\n\nThis exists because a placeholder `amount: 0` is a lie the contract cannot distinguish from genuine free: Workflow Lens at zero really is free, M365 Plus at zero was a stand-in for an undecided price, and a consumer reading catalog.json saw the same thing. The whole point of always stating a price was that absent and free must not look alike; a placeholder zero reintroduced exactly that collision one level down.",
          "properties": {
            "amount": {
              "type": "number",
              "minimum": 0,
              "description": "A decided price. Zero means genuinely free."
            },
            "status": {
              "type": "string",
              "enum": ["undecided", "on-request"],
              "description": "`undecided` — not yet priced, and must not be sold. `on-request` — deliberately negotiated per customer, as a site licence is; that is a decided commercial position, not a gap."
            },
            "currency": { "type": "string", "enum": ["EUR", "USD"] }
          },
          "oneOf": [
            { "required": ["amount"], "not": { "required": ["status"] } },
            { "required": ["status"], "not": { "required": ["amount"] } }
          ]
        },
        "duration": {
          "type": "string",
          "oneOf": [
            { "enum": ["perpetual", "annual", "monthly"] },
            { "pattern": "^[1-9][0-9]*d$", "description": "An explicit number of days, e.g. 30d." }
          ],
          "description": "A licencing period is not always a calendar one: the free tier lapses every 30 days, which is neither monthly nor annual, and the enforcement layer counts seconds. Named durations stay for the common cases."
        },
        "renewal": {
          "type": "string",
          "enum": ["none", "renew", "reissue"],
          "description": "What happens at the end of `duration`. `renew` extends the same licence and keeps its key; `reissue` replaces it with a new one, which is the only way to move a holder onto different terms. `none` means it simply lapses."
        },
        "status": {
          "type": "string",
          "enum": ["active", "planned", "on-demand", "retired"],
          "description": "Whether this offer can be bought today. A retired SKU stays in the file forever because orders still reference it."
        },
        "eligible_for": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string" },
          "description": "Who may take this offer, in the vendor's own vocabulary (e.g. Community, ProOrEnterprise). A commercial condition, not an enforcement mechanism."
        },
        "channels": {
          "type": "array",
          "description": "Where this offer can be obtained, and between which dates. An offer with no open channel is not purchasable, whatever its status says.",
          "items": {
            "type": "object",
            "required": ["channel"],
            "additionalProperties": false,
            "properties": {
              "channel": { "type": "string", "enum": ["manual", "self-service", "gumroad", "stripe"] },
              "valid_from": { "type": "string", "format": "date" },
              "valid_thru": { "type": "string", "format": "date" }
            }
          }
        },
        "highlights": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Storefront bullets. Prose for a human, never read by code."
        },
        "seat_model": { "type": "string", "enum": ["single-user", "per-machine", "site", "unlimited-machines"] },
        "seats": { "type": "integer", "minimum": 1, "description": "Required by per-machine; meaningless otherwise." },
        "grants": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string" },
          "description": "Validated against data/entitlements.yaml, so a typo cannot silently grant nothing."
        },
        "artefacts": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Which of the product's artefacts this offer delivers, by their ids. REQUIRED whenever the product has any — validate.py enforces that, because a regex cannot see the product from inside an offer.\n\nIt used to mean 'omit for all of them', which was harmless while a product had one artefact and silently wrong once it had two. It also shipped: the Workflow Lens offer published no artefacts, so nothing could answer what its SKU delivered. Never infer; say which."
        },
        "note": { "type": "string" }
      }
    }
  }
}
